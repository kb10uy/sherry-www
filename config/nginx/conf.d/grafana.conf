server {
    server_name grafana.hinanawi.net;
    listen 443 ssl http2;

    ssl_certificate /ssl/hinanawi-net.crt;
    ssl_certificate_key /ssl/hinanawi-net.key;
    ssl_dhparam /ssl/dhparam.pem;

    location / {
        proxy_pass http://grafana:3000/;
    }
}
